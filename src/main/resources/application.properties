# Application Configuration
quarkus.application.name=go-game-server

# HTTP Configuration
quarkus.http.port=4714
quarkus.http.cors=true
quarkus.http.cors.origins=http://localhost:3000,http://localhost:5173
quarkus.http.cors.headers=accept,authorization,content-type,x-requested-with
quarkus.http.cors.methods=GET,POST,PUT,DELETE,OPTIONS
quarkus.http.cors.exposed-headers=Content-Disposition

# JWT Configuration
# The signing key is base64-encoded (32 bytes / 256 bits for HS256).
# IMPORTANT: For production deployment, generate a new secure key and store it in environment
# variables or a secrets manager. Never commit production keys to version control!
mp.jwt.verify.issuer=go-game-server
smallrye.jwt.sign.key=O+5OGvGw9TtAwbuEuUh/CZh7dpb84RyK8oXjdnLHfLQ=
smallrye.jwt.verify.key.location=jwt-secret.jwk
smallrye.jwt.verify.algorithm=HS256

# Custom JWT settings
jwt.expiration.hours=24

# Data Storage Configuration
app.data.directory=../../../../data

# GNU Go AI Configuration
ai.gnugo.enabled=true
# Relative path from project root - works on both Windows and Linux
ai.gnugo.base.dir=../../../../ai/gnugo
ai.gnugo.executable.name.windows=gnugo.exe
ai.gnugo.executable.name.linux=gnugo

# AI bot usernames and their corresponding difficulty levels (format: username:level)
# GNU Go levels: 1-10 (higher = stronger but slower)
ai.bots.config=GnuGo-Easy:2,GnuGo-Casual:4,GnuGo-Medium:6,GnuGo-Hard:8,GnuGo-Max:10

# GTP settings
ai.gtp.command.timeout=30000
ai.gtp.process.restart.maxAttempts=3

# Logging
quarkus.log.level=INFO
quarkus.log.category."net.tfassbender.game".level=DEBUG
quarkus.log.console.format=%d{HH:mm:ss} %-5p [%c{2.}] (%t) %s%e%n

# Development mode settings
%dev.quarkus.http.port=4714
%dev.quarkus.log.console.enable=true
